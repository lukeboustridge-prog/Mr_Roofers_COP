---
phase: 34-installation-guide-transformation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layout/Sidebar.tsx
  - components/layout/MobileNav.tsx
  - components/layout/ModeToggle.tsx
  - components/search/CommandSearch.tsx
  - components/navigation/Breadcrumbs.tsx
  - app/(dashboard)/fixer/page.tsx
  - app/(dashboard)/fixer/results/page.tsx
  - app/(dashboard)/fixer/[detailId]/page.tsx
  - app/(dashboard)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Sidebar shows 'Installation Guide' instead of 'Fixer' with appropriate icon"
    - "Mobile bottom nav shows 'Guide' instead of 'Fixer'"
    - "Mobile sidebar sheet shows 'Installation Guide' instead of 'Fixer Mode'"
    - "Mode toggle shows 'Guide' instead of 'Fixer'"
    - "Home page card shows 'Installation Guide' instead of 'Fixer Mode'"
    - "Breadcrumbs on detail pages show 'Installation Guide' instead of 'Fixer'"
    - "Command search shows 'Installation Guide' instead of 'Fixer'"
    - "Fixer landing page heading says 'Installation Guide' with updated description"
    - "Results page breadcrumbs say 'Installation Guide'"
  artifacts:
    - path: "components/layout/Sidebar.tsx"
      provides: "Renamed nav item label"
      contains: "Installation Guide"
    - path: "components/layout/MobileNav.tsx"
      provides: "Renamed mobile nav labels"
      contains: "Installation Guide"
    - path: "app/(dashboard)/fixer/page.tsx"
      provides: "Renamed landing page heading"
      contains: "Installation Guide"
  key_links:
    - from: "components/layout/Sidebar.tsx"
      to: "/fixer"
      via: "href attribute"
      pattern: "href.*fixer"
    - from: "components/navigation/Breadcrumbs.tsx"
      to: "fixer label"
      via: "pathLabelMap"
      pattern: "fixer.*Installation Guide"
---

<objective>
Rename all user-facing "Fixer" references to "Installation Guide" across navigation, headings, breadcrumbs, and page titles.

Purpose: INSTALL-01 requires Fixer mode to be rebranded as "Installation Guide" to better convey its purpose as a practical, detail-centric installation reference.
Output: All UI text referencing "Fixer" or "Fixer Mode" updated to "Installation Guide" (or "Guide" where space-constrained like mobile nav).
</objective>

<execution_context>
@C:/Users/LukeBoustridge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LukeBoustridge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@components/layout/Sidebar.tsx
@components/layout/MobileNav.tsx
@components/layout/ModeToggle.tsx
@components/search/CommandSearch.tsx
@components/navigation/Breadcrumbs.tsx
@app/(dashboard)/fixer/page.tsx
@app/(dashboard)/fixer/results/page.tsx
@app/(dashboard)/fixer/[detailId]/page.tsx
@app/(dashboard)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Fixer to Installation Guide in navigation components</name>
  <files>
    components/layout/Sidebar.tsx
    components/layout/MobileNav.tsx
    components/layout/ModeToggle.tsx
    components/search/CommandSearch.tsx
    components/navigation/Breadcrumbs.tsx
  </files>
  <action>
    Update all user-facing "Fixer" text to "Installation Guide" (or "Guide" in space-constrained contexts):

    1. **Sidebar.tsx** (line 29):
       - Change label from `'Fixer'` to `'Installation Guide'`
       - Keep icon as `Wrench` (represents hands-on installation work)
       - Keep href as `/fixer` (route renaming is Phase 35 migration concern)

    2. **MobileNav.tsx**:
       - Bottom nav (line 21): Change label from `'Fixer'` to `'Guide'` (space-constrained mobile bottom bar)
       - Mobile sidebar (line 88): Change label from `'Fixer Mode'` to `'Installation Guide'`
       - Mode indicator at bottom of mobile sidebar (line 180): Change `'Fixer'` to `'Guide'`

    3. **ModeToggle.tsx** (line 34):
       - Change `'Fixer'` to `'Guide'` (compact toggle button text)

    4. **CommandSearch.tsx** (line 31):
       - Change label from `'Fixer'` to `'Installation Guide'`
       - Update keywords array to include `'install', 'guide', 'installation'`

    5. **Breadcrumbs.tsx** (line 168):
       - Change pathLabelMap entry from `'fixer': 'Fixer'` to `'fixer': 'Installation Guide'`

    Do NOT rename route paths (/fixer stays /fixer) — route renaming happens in Phase 35.
    Do NOT rename internal variable/function names (fixerContext, isFixerMode, etc.) — they are internal API.
  </action>
  <verify>Run `npm run build` to confirm no compilation errors. Grep for remaining user-visible "Fixer" strings: `grep -rn "Fixer" components/layout/ components/search/ components/navigation/` should show zero user-visible label matches (only internal code references like variable names are acceptable).</verify>
  <done>All 5 navigation components show "Installation Guide" (or "Guide" where space-constrained) instead of "Fixer" in their rendered output.</done>
</task>

<task type="auto">
  <name>Task 2: Rename Fixer to Installation Guide in page components</name>
  <files>
    app/(dashboard)/fixer/page.tsx
    app/(dashboard)/fixer/results/page.tsx
    app/(dashboard)/fixer/[detailId]/page.tsx
    app/(dashboard)/page.tsx
  </files>
  <action>
    Update all user-facing "Fixer" text in page-level components:

    1. **app/(dashboard)/fixer/page.tsx**:
       - Line 33: Change heading from `'Fixer Mode'` to `'Installation Guide'`
       - Line 37: Update description from "Quick lookup - select your context to find relevant details. Use voice input or tap to select." to "Find installation details by substrate and task type. Select your context to browse relevant 3D models, steps, and warnings."

    2. **app/(dashboard)/fixer/results/page.tsx**:
       - Line 234: Change breadcrumb label from `'Fixer'` to `'Installation Guide'`

    3. **app/(dashboard)/fixer/[detailId]/page.tsx**:
       - Line 70: Change breadcrumb label from `'Fixer'` to `'Installation Guide'`

    4. **app/(dashboard)/page.tsx** (home page):
       - Line 147: Change CardTitle from `'Fixer Mode'` to `'Installation Guide'`
       - Update the description text below the title to mention installation details, 3D models, and step-by-step instructions instead of "quick lookup" language
       - Keep the Wrench icon and secondary color scheme (practical/on-site connotation)

    Do NOT rename route paths or internal variable names.
  </action>
  <verify>Run `npm run build` to confirm no compilation errors. Grep for remaining "Fixer Mode" strings: `grep -rn "Fixer Mode" app/` should return zero matches.</verify>
  <done>All 4 page components show "Installation Guide" instead of "Fixer Mode" in their rendered output. Landing page has updated description emphasizing practical content.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `grep -rn '"Fixer"' components/layout/ components/search/ components/navigation/ app/\(dashboard\)/fixer/ app/\(dashboard\)/page.tsx` returns only internal code references (variable names), not user-facing labels
3. `grep -rn "Fixer Mode" .` returns zero matches in UI-facing strings
</verification>

<success_criteria>
- Every user-facing occurrence of "Fixer" or "Fixer Mode" in the application is replaced with "Installation Guide" (or "Guide" in mobile-constrained contexts)
- Internal code identifiers (fixerContext, isFixerMode, /fixer routes) remain unchanged for backward compatibility
- Application builds successfully with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/34-installation-guide-transformation/34-01-SUMMARY.md`
</output>
