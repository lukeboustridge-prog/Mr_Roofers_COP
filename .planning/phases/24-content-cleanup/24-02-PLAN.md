---
phase: 24-content-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/clean-htg-content.ts
autonomous: true

must_haves:
  truths:
    - "HTG content records free of page numbers, headers/footers, section number artifacts"
    - "Technical content accuracy preserved - no semantic changes"
    - "Script is repeatable for future content updates"
  artifacts:
    - path: "scripts/clean-htg-content.ts"
      provides: "HTG database content cleanup script"
---

<objective>
Clean HTG database content records of PDF extraction artifacts while preserving technical accuracy.
350 records across 3 source documents (flashings, penetrations, cladding).
</objective>

<tasks>

<task type="auto">
  <name>Create and run HTG content cleanup script</name>
  <files>scripts/clean-htg-content.ts</files>
  <action>
Create TypeScript script to clean all HTG content records in database.
Connect via Drizzle ORM, fetch all htg_content records, clean content field,
batch update back to database. Remove page numbers, headers/footers, 
section artifacts, normalize whitespace.
  </action>
</task>

</tasks>
